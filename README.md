# Lab2-SO3-Comands

Práctica1
sudo apt update && sudo apt upgrade -y
grep -r ^deb /etc/apt/sources.list /etc/apt/sources.list.d/
apt-cache search bashtop
sudo apt install bashtop
bashtop
bashtop
sudo apt purge bashtop && sudo apt autoremove -y

Práctica2
sudo crontab -e
1
0 23 * * * sudo apt update && apt upgrade -y
sudo crontab -e
0 3 * * 0 sudo reebot
echo "rm -rf /tmp/*" | at now + 1 minute
atq
cd /tmp
ls -l
cd VMwareDnD/
ls -l
cd ..
sudo su
echo "rm -rf /tmp/*" | at now + 1 minute
ls -l

cd
